package movie.rw.MovieManagementSystem.model;

/**
 * Enum representing the hierarchical types of locations.
 * PROVINCE → DISTRICT → SECTOR forms the location hierarchy.
 */
public enum ELocationType {
    PROVINCE,
    DISTRICT,
    SECTOR
}
